Refactor demo.dudilig.com landing page for tighter information density and better hierarchy. Keep the dark navy background, Inter or similar geometric sans, electric blue accent (#3b82f6), shield mark, and the "AGENTIC COMPLIANCE OS" pill. The current page has three structural problems I want you to fix without changing the brand or color palette.

PROBLEM 1: VERTICAL SPACING IS TOO LOOSE.
The page reads as a tall column with excessive whitespace between every section. Tighten as follows:
- Reduce padding-top on the hero from current value to roughly 80px on desktop, 48px on mobile
- Reduce the gap between subhead and CTA buttons by ~40%
- Reduce the gap between CTA buttons and "LIVE TRUST CERTIFICATES" section by ~50%
- Cut the gap between trust certificate cards and the feature checklist by ~60%
- Remove the empty space below the feature checklist; the "DEMO ENVIRONMENT · MERIDIAN CAPITAL PARTNERS" footer should sit immediately below

PROBLEM 2: TRUST CERTIFICATE CARDS HAVE WEAK HIERARCHY.
Right now the score (88/100) and status (PASS) compete with the title for visual weight. Restructure each card to:
- Status pill (PASS/REVIEW/MISMATCH) becomes smaller, top-left corner only, no big icon
- Move score (88/100) to top-right as a smaller monospace number, color-matched to status
- Title (e.g., "Meridian Private Credit Fund I") becomes the dominant element, larger and bolder than current
- Subtitle line ("ERC-1400 Security Token · Meridian Capital Partners") stays small and muted
- Remove the bottom status text ("Clean ERC-1400 pass →") entirely; the card itself is clickable
- Add subtle hover state: card border lifts to electric blue, no other animation
- Cards should have equal heights; if titles wrap to 2 lines on one card, others match

PROBLEM 3: FEATURE CHECKLIST IS FILLER.
The four-line checklist at the bottom (Continuous KYC/AML, Smart contract attestation, Regulatory filing tracker, Investor registry) duplicates information already implied by the trust certificates above and adds rows without adding meaning. Replace the entire checklist block with a single line of small, muted text directly under the trust certificate cards, reading exactly:

"Every certificate above was generated in real time by an AI agent reading the actual smart contract and offering documents. Verify any of them →"

Make "Verify any of them →" an electric blue link styled the same as "Publish yours →" in the row above.

ADDITIONAL REFINEMENTS:
- The hero subhead currently reads "Automate KYC/AML, smart contract attestation, and regulatory filings for your RWA issuance." Tighten to: "KYC, smart contract attestation, and filings — generated and verified by AI agents."
- Move "Publish yours →" link from the right edge of the LIVE TRUST CERTIFICATES heading row to a position immediately right of "LIVE TRUST CERTIFICATES" with a vertical separator pipe. Same line, less visual stretching.
- The "DEMO ENVIRONMENT · MERIDIAN CAPITAL PARTNERS" footer currently sits orphaned at the bottom. Wrap it in a subtle 1px top border (color #1f2937, full page width) and add a second line below in same muted style: "demo.dudilig.com · not for production use"
- Standardize all section padding to a consistent rhythm: 64px between major sections on desktop, 32px on mobile. No section should have more than 96px above or below.

OUTPUT: Updated React/Next.js page component with Tailwind classes, no inline styles, no commented-out old code. Use semantic HTML (section, header, footer). The page should fit in a single laptop screen above the fold (1440x900) without scrolling to see at least one trust certificate card.