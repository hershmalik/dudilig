Build the hero section for demo.dudilig.com. Use the Membrane interactive hero pattern as a starting point but adapt for an institutional B2B compliance platform serving tokenized real-world asset issuers. The goal is institutional polish, not creative-portfolio drama. Adopt the staggered reveal animation and typography system; replace the interactive mesh with a static institutional treatment; rewrite all content for compliance-buyer audience.

KEEP FROM MEMBRANE TEMPLATE:
- Section: 100vh, 100vw, overflow hidden, background #08080a
- Typography: Playfair Display for the title, Geist Sans for everything else
- Cinematic content reveal animation: opacity 0 → 1, y: 30px → 0, scale: 0.94 → 1, blur: 6px → 0px, GSAP power4.out easing, 1s duration, 130ms stagger between elements, 500ms initial delay
- Reveal order: badge → title → subtitle → primary CTA + secondary CTA → trust certificate cards
- Centered content layout, max-width container
- All elements opt into pointer-events appropriately

DO NOT INCLUDE FROM MEMBRANE:
- The 50x30 vertex mesh canvas. No interactive physics. No spring system. No mouse-following effects.
- The "Touch the invisible" / "A living surface" copy paradigm
- The avatar stack social proof with "2,400+ creators"
- The "View code" secondary button
- The award badge with laurels and stars

REPLACE THE MESH BACKGROUND WITH THIS:
A subtle radial gradient centered behind the title, fading from rgba(59, 130, 246, 0.06) at center to transparent at edges, radius approximately 60% of viewport width. Static, no animation. Optionally: a very subtle 1px grid pattern at rgba(255, 255, 255, 0.015) opacity overlaid on the entire section. The grid is purely decorative texture, not interactive.

CONTENT SPECIFICATION:

Brand mark (top, smaller than current):
- Shield icon + "Dudilig" wordmark, monospace font for wordmark
- Position: top-left of the section, padding 32px from top and left edges
- Color: white at 60% opacity
- Click target: links to dudilig.com (the marketing site)

Pill badge (above the title, small):
- Text: "AGENTIC COMPLIANCE OS"
- Background: rgba(255, 255, 255, 0.04), border 1px solid rgba(255, 255, 255, 0.08), rounded-full
- Padding: 6px 14px, font-size 11px, letter-spacing 0.1em, monospace font
- Includes a small green dot (4px, #22c55e) at the start of the text with subtle pulse animation (scale 1.0 → 1.2, 2s duration, infinite, ease-in-out)
- Color: white at 70% opacity

Title (the dominant element):
- Text: "The Compliance OS for Tokenized Assets"
- Font: Playfair Display, font-weight 500
- Size: clamp(2.8rem, 6.5vw, 5.5rem)
- Line-height: 1.05
- Letter-spacing: -0.02em
- Color: white
- The phrase "Tokenized Assets" should be styled differently: same font and size, but with a subtle gradient from #3b82f6 to #6366f1 (blue-to-indigo, very narrow range — this should look like a single accent color, not a rainbow)
- Margin-top: 24px from badge

Subtitle:
- Text: "KYC, smart contract attestation, and regulatory filings — generated and verified by AI agents reading the actual contract."
- Font: Geist Sans, 15px, line-height 1.55
- Color: rgba(255, 255, 255, 0.45)
- Max-width: 540px, centered
- Margin-top: 20px from title

CTA buttons (two side by side, gap 12px):

Primary CTA "Get Started":
- Background: white, color #08080a, rounded-full
- Padding: 12px 28px, font-size 14px, font-weight 500
- Hover: box-shadow 0 0 40px rgba(255, 255, 255, 0.12), scale 1.02
- Right-arrow icon (lucide-react ArrowRight, 16px) with margin-left 6px
- Margin-top: 32px from subtitle

Secondary CTA "View Demo":
- Background: rgba(255, 255, 255, 0.04), border 1px solid rgba(255, 255, 255, 0.1), rounded-full
- Color: rgba(255, 255, 255, 0.85)
- Same padding and font-size as primary
- Hover: border brightens to rgba(255, 255, 255, 0.25), background to rgba(255, 255, 255, 0.06)
- Right-arrow icon, same as primary

LIVE TRUST CERTIFICATES section (replaces social proof, sits below CTAs):

Section header row:
- Margin-top: 72px from CTA buttons (large gap to separate hero from proof section)
- Left side: small uppercase label "LIVE TRUST CERTIFICATES" in monospace, 11px, letter-spacing 0.15em, color rgba(255, 255, 255, 0.4)
- Right side: link "Publish yours →" in electric blue (#3b82f6), 13px, regular weight, hover brightens to lighter blue
- Subtitle line below the header (left-aligned, full width): "Public, signed compliance attestations — generated by reading the actual contract through Claude Opus 4.5." in Geist Sans 13px, color rgba(255, 255, 255, 0.4)
- Margin-top of subtitle: 8px from header row

Three trust certificate cards (grid, 3 columns, gap 16px, margin-top 20px from header):

Each card:
- Background: rgba(255, 255, 255, 0.025), border 1px solid rgba(255, 255, 255, 0.08), border-radius 16px
- Padding: 24px
- Hover: border brightens to rgba(59, 130, 246, 0.4), no scale change, transition 200ms

Card top row (status pill + score):
- Status pill (left): small rounded-full badge, padding 4px 10px, font-size 10px, letter-spacing 0.1em, monospace font, uppercase
  - PASS: background rgba(34, 197, 94, 0.1), border 1px solid rgba(34, 197, 94, 0.3), text #22c55e
  - REVIEW: background rgba(234, 179, 8, 0.1), border 1px solid rgba(234, 179, 8, 0.3), text #eab308
  - MISMATCH: background rgba(239, 68, 68, 0.1), border 1px solid rgba(239, 68, 68, 0.3), text #ef4444
- Score (right): monospace font, 13px, format "88/100", color matched to status pill color but at 80% opacity

Card title (margin-top 14px from top row):
- Font: Geist Sans, 16px, font-weight 600, color white
- Line-height: 1.3, max 2 lines

Card subtitle (margin-top 6px from title):
- Font: Geist Sans, 12px, color rgba(255, 255, 255, 0.45)
- Single line, truncate with ellipsis if too long
- Format: "[Token Standard] · [Issuer Name]"

Card data:
1. Status: PASS, Score: 88/100, Title: "Meridian Private Credit Fund I", Subtitle: "ERC-1400 Security Token · Meridian Capital Partners"
2. Status: REVIEW, Score: 58/100, Title: "Aspen Ranchland REIT Series A", Subtitle: "Reg D 506(c) · Aspen Capital Group"
3. Status: MISMATCH, Score: 32/100, Title: "Gulf Real Estate Income Trust", Subtitle: "Reg D 506(c) · Gulf Sovereign Holdings"

Below the three cards, a single muted line (margin-top 20px):
- Text: "Every certificate above was generated in real time by an AI agent reading the actual smart contract and offering documents. Verify any of them →"
- Font: Geist Sans, 12px, color rgba(255, 255, 255, 0.4)
- "Verify any of them →" is an electric blue link in the same color as "Publish yours →"

Section footer (bottom of section, full width):
- Margin-top: 64px from the verification line
- 1px top border, color rgba(255, 255, 255, 0.06)
- Padding-top: 20px
- Two lines:
  - Line 1: "DEMO ENVIRONMENT · MERIDIAN CAPITAL PARTNERS" in monospace, 10px, letter-spacing 0.15em, color rgba(255, 255, 255, 0.3), centered
  - Line 2: "demo.dudilig.com · not for production use" in Geist Sans, 10px, color rgba(255, 255, 255, 0.25), centered
  - Gap between lines: 6px

RESPONSIVE BEHAVIOR:
- Mobile (<768px): trust certificate cards stack vertically, gap 12px between them; title scales via clamp; section header row stacks (label on top, link below)
- Tablet (768-1024px): trust certificate cards remain 3 columns but with smaller padding (16px instead of 24px)
- Desktop (>1024px): full layout as specified

PERFORMANCE & TECH:
- React/Next.js with Tailwind classes, no inline styles except where necessary for dynamic values
- GSAP for the entrance animation only, no other animation library
- Use lucide-react for icons (ArrowRight, ShieldCheck for the brand mark)
- All static content — no API calls, no data fetching
- Total component should be under 250 lines

OUT OF SCOPE FOR THIS BUILD:
- Click handlers on trust certificate cards (links can be href="#" placeholders)
- The "Publish yours" flow
- The "Verify any of them" flow
- The "View Demo" flow
- Authentication

Output: a single React component file with all Tailwind classes inline. Use semantic HTML (header for brand mark, section for hero, footer for the demo environment block).