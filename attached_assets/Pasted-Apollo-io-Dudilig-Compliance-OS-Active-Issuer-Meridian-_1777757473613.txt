Apollo.io
Dudilig
Compliance OS

Active Issuer

Meridian Capital Partners

Cayman Islands · 3 Tokens

Dashboard
Investors
Attestation
Contract Analyzer
Filings
Tokens
Compliance AI
Trust Certificate
Credits
Platform tier
1,753 remaining
5,000 / mo
Top up credits
MC
Meridian Admin

admin@meridian.ky

Contract Analyzer
Paste Solidity code · get live compliance analysis powered by Claude


Live AI analysis — no mocks

Paste any ERC-1400, ERC-3643, or custom security token contract. Claude reads the actual Solidity and checks every compliance rule for the standard you select. Results are real — Pass/Fail/Warning per rule with specific code references.

Compliance Standard


Reg D 506(c)

US accredited investors


Reg S

Offshore, non-US only


MiCA

EU crypto assets


ERC-1400

Security token standard

Solidity Contract

Load example
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.19;

import "@openzeppelin/contracts/token/ERC1400/ERC1400.sol";
import "./interfaces/ICompliance.sol";

contract MeridianPrivateCreditFund is ERC1400 {

    uint256 public constant MAX_INVESTORS = 250;
    uint256 public constant TRANSFER_LOCKUP = 365 days;
    bool public constant ACCREDITED_ONLY = true;

    string[] public jurisdictionWhitelist = [
        "KY", "SG", "AE", "HK", "VG", "BVI"
    ];

    mapping(address => bool) private _accreditedInvestors;
    mapping(address => uint256) private _investorSince;
    uint256 private _totalInvestors;

    ICompliance private _compliance;

    modifier onlyAccredited(address investor) {
        require(
            _accreditedInvestors[investor],
            "Investor not accredited"
        );
        _;
    }

    modifier withinInvestorCap() {
        require(
            _totalInvestors < MAX_INVESTORS,
            "Investor cap reached"
        );
        _;
    }

    function _beforeTokenTransfer(
        address from,
        address to,
        uint256 amount
    ) internal override {
        if (from != address(0)) {
            require(
                block.timestamp >= _investorSince[from] + TRANSFER_LOCKUP,
                "Transfer locked"
            );
        }
        require(
            _compliance.isJurisdictionAllowed(to),
            "Jurisdiction not permitted"
        );
        super._beforeTokenTransfer(from, to, amount);
    }
}
56 lines

Run Analysis
Analysis Result — Reg S

Copy report
58
/ 100
NEEDS REVIEW
The contract implements partial Reg S compliance with a 365-day transfer lockup and jurisdiction whitelist delegation to an external compliance contract. However, it lacks explicit US person blocking mechanisms and the jurisdiction whitelist includes only non-US jurisdictions without explicit US exclusion logic. Critical gaps exist in enforcing US investor exclusion at the contract level.

1 passed
4 warnings
0 failed
Critical Issues

• jurisdictionWhitelist array is declared but never used in actual transfer validation logic

• No explicit US person/jurisdiction blocking implemented at contract level - entirely dependent on external ICompliance contract

• _investorSince mapping is never populated in visible code - lockup enforcement may fail if not set during minting

• External _compliance contract is never initialized in visible code

Rule-by-Rule Findings


US investor exclusion — wallet addresses or identity checks must block US persons

WARNING

Offshore transaction conditions — distribution outside US markets only

WARNING

Directed selling efforts prohibition — no active marketing to US investors

NA

Restricted period compliance (40-day for equity, 1-year for debt)

PASS

Transfer restrictions to US persons during restricted period

WARNING

Jurisdiction whitelist enforcement in transfer logic

WARNING
Recommendations

1.
Implement explicit US jurisdiction blocking with on-chain verification rather than relying solely on external contract

2.
Add setter function or constructor initialization for _compliance contract address with appropriate access controls

3.
Ensure _investorSince[investor] is set during token minting/issuance in _beforeTokenTransfer when from == address(0)

4.
Consider using the declared jurisdictionWhitelist directly in transfer logic or remove unused state variable

5.
Add explicit require statement blocking transfers to addresses with 'US' jurisdiction classification

6.
Implement investor KYC/AML status checking beyond just accreditation for Reg S compliance